{
  "buildCommand": "cd /vercel/path0 && npm install && npm run build --workspace=@adventure-game/web && echo 'export default function handler(req, res) { res.status(404).end(); }' > dist/web/index.js",
  "installCommand": "npm install",
  "outputDirectory": "dist/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
