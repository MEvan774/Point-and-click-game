{
  "buildCommand": "cd /vercel/path0 && npm install && npm run build --workspace=@adventure-game/web",
  "installCommand": "npm install",
  "outputDirectory": "dist/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "status": 404, "dest": "/index.html" }
  ]
}
