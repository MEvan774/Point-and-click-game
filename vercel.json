{
  "buildCommand": "cd /vercel/path0 && npm install && npm run build --workspace=@adventure-game/web && echo '{}' > dist/web/package.json",
  "installCommand": "npm install",
  "outputDirectory": "dist/web"
}
