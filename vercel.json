{
  "buildCommand": "cd /vercel/path0 && npm install && npm run build --workspace=@adventure-game/web && mkdir -p public && cp -r dist/web/* public/",
  "installCommand": "npm install",
  "outputDirectory": "public"
}
